# Esmiuçando o range: range(inicio do intervalo, fim do intervalo excluíndo o último número,
# valor do incremento neste caso a cada 5)
for i in range(0, 31, 5):  # 10 é o número de parada do looping, ele não é incluso.
    print(f"Variável i valendo: {i}")
